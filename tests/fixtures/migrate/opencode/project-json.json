{
  "mcp": {
    "filesystem": {
      "command": ["npx", "-y", "@modelcontextprotocol/server-filesystem", "${WORKSPACE_ROOT}"],
      "env": {
        "LOG_LEVEL": "${LOG_LEVEL}"
      },
      "enabled": true,
      "type": "local"
    }
  }
}
